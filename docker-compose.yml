services:
  actualtap:
    container_name: actualtap
    image: mattyfaz/actualtap:latest
    ports:
      - 3001:3001
    environment:
      - ACTUAL_URL=
      - ACTUAL_PASSWORD=
      - ACTUAL_BUDGET_ID=
      - API_KEY=
    volumes:
      - /your/path/here:/app/data
    network_mode: bridge
