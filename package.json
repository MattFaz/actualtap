{
  "dependencies": {
    "@actual-app/api": "^26.1.0",
    "@fastify/cors": "^11.2.0",
    "@fastify/env": "^5.0.3",
    "fastify": "^5.7.2",
    "fastify-plugin": "^5.1.0",
    "pino-pretty": "^13.1.3"
  },
  "name": "actualtap",
  "version": "1.0.23",
  "description": "Automatically create transactions in Actual Budget when you use Tap-to-Pay on a mobile device",
  "main": "index.js",
  "repository": "https://github.com/MattFaz/actualtap",
  "author": "Matt Farrell",
  "license": "GPL-3.0",
  "private": false,
  "devDependencies": {
    "nodemon": "^3.1.11"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --ignore ./data/ --ignore ./bruno/ --ignore ./test/ node src/server.js",
    "test": "node --test test/*.test.js",
    "test:watch": "node --test --watch test/*.test.js"
  }
}
